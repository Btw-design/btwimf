# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Claude Code local tooling
.claude/

# Runtime: stored form submissions (created by form-handler.php on the server)
_submissions/

# Blog admin — content + config live on the server, managed via /admin/
/admin/config.php
/_blog-data/
/assets/img/blog/
/blogs/*/
/blogs/feed.xml
